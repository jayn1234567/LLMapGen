# Install torch/torchvision/torchaudio separately for the target hardware.
# GPU example: choose the CUDA wheel matching the local driver/runtime.
# NPU example: install the matching torch-npu / CANN stack from the Ascend source.
# torch==2.7.0
# torchvision
# torchaudio

# Core model stack.
transformers==4.56.2
huggingface-hub==0.36.2
tokenizers>=0.19.0
sentencepiece>=0.1.99
tiktoken>=0.7.0
accelerate>=0.20.0
peft>=0.10.0
safetensors>=0.4.3
bitsandbytes

# Multimodal / vision utilities.
numpy>=1.26
Pillow>=10.0.0
opencv-python-headless>=4.8.0
einops>=0.6
einops-exts>=0.0.4
timm>=0.9.0
scikit-learn>=1.2

# Data processing and geometry evaluation.
tqdm>=4.66.0
loguru>=0.7.0
shapely>=2.0.0
geopandas>=0.14.0
rasterio>=1.3.0
pyproj>=3.6.0
fiona>=1.9.0

# Training / distributed runtime.
deepspeed==0.13.1
ninja
wandb
swanlab

# RL rollout stack.
ray[default]==2.55.1
vllm==0.9.2

# Service / debug UI helpers kept by the upstream framework.
gradio>=5.0
requests
uvicorn
fastapi
pydantic
markdown2[all]
shortuuid
